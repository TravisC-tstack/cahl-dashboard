# JURY 3/5 — VISUAL CRAFT LENS — CAHL Dashboard v50 (cahl.neural-forge.io)

Probed live: Today/League/Team/Players/Analytics desktop 1440x900, dark+light themes, 390x844 mobile, Cmd+K, font-loading API, computed styles. Screenshots: qa/jury3_desktop_today.png, qa/jury3_league.png, qa/jury3_mobile.png.

| # | Dimension | Evidence (what I saw) | Score |
|---|-----------|------------------------|-------|
| 1 | Visual identity | Broadcast gestures are real (mono scoreboard labels, condensed team names, rink pills, cut-line, LIVE pill) but the marquee card is broken: hero team name renders clipped as "…eys Hockey" (DOM text "Swamp Donkeys Hockey" at x=-63, absolutely positioned outside the card, colliding with subtitle) and "0–0 @" dangles with no opponent. First impression is broken, not broadcast. | 6.5 |
| 2 | Typographic system | Fonts DO load and DO reach scores: Saira Condensed 600/700 loaded and applied to .t-score/.sb-num/hero-record (document.fonts.check true); JetBrains Mono on all eyebrow labels/headers/times. tnum coverage broad (table, .t-score, .pts, .kpi-num). BUT numerals only ever appear as "0" on season start, so the display type reads nowhere on Today (t-score cells show mono "vs"); .today-cols-head header NOT mono (Inter lowercase, renders "TimeHomeScoreAwayRink" concatenated on mobile); mono sizes scatter 8.5-11px across 6 steps. | 6.0 |
| 3 | Color system | Core discipline holds — Goal Red #CE1126 counted on exactly 12 elements, reserved for live/cut/active/section bullets; link blue rgb(42,127,212) is off-token (Union Blue #002654 exists in :root but is unused); hero-card radial blue tint + amber "My Team" chip + blue→purple→red top gradient strip each add a hue the system never uses again. ONE accent discipline = 80% true; the remaining 20% is exactly what the brief says not to ship. | 6.5 |
| 4 | Layout & hierarchy | STRUCTURALLY BROKEN: .today-list is grid 569px/569px but .today-cols-head spans cell 1 only, so SCORE floats over the left group's rink pills and AWAY over the right group's TIME column; right group's TIME/HOME have no headers. Near-zero gutter seam between the two groups. Mobile repeats it: full 5-label header over stacked cards + concatenated "TimeHomeScoreAwayRink". Hero overlaps subtitle on desktop AND mobile. Empty .session-records module (label, no content). | 5.5 |
| 5 | Density & data presentation | Best dimension: sticky theads verified on League/Players, tabular-nums on table, hairline row separators, day/division chips, PLAYOFF LINE divider after row 8, sort arrow on PTS. But no zebra, Season Heat values butt against names ("The Crue0", unaligned), Goal Diff chart is a 6px sliver contradicting its own legend, and leaderboard is a stub "Loading…" with no skeleton. | 7.0 |
| 6 | Interaction & micro-feedback | Cmd+K palette renders clean with keyboard hints; nav links 49px tall; rows are full links. But synthetic Cmd+K did NOT open the palette (only real user gesture does — verified via DOM, flagging as unverifiable-in-probe rather than broken); team-link click produced no visible pressed/active feedback before navigation; no skeleton loaders anywhere (5 observed "Loading…" text-only states). | 7.0 |
| 7 | Motion design | Restrained: fade-in entrance, LIVE pulse + score flash defined, 3 prefers-reduced-motion blocks present in CSS. Nothing decorative found (0 canvas/SVG charts to ornament). Slightly under-animated: zero motion differentiation on tab switches. | 7.5 |
| 8 | Depth & material | Genuinely flat, hairline-based: 4 distinct shadows total, 1 backdrop-filter remnant, luminance-stacked navy cards on dark theme read correctly; light theme equally flat. No blur soup, no fake depth. Only lapse: top gradient strip ornament. | 7.5 |
| 9 | Consistency & component quality | One scorecell anatomy exists in CSS but is contradicted in DOM: status chips (UPCOMING) sit in the SCORE column with mono vs the display-font score spec; chip radius/heights consistent; mono label tracking inconsistent (8.5/9.5/10/10.5/11/11.9px); native <select> on Team tab is default-styled (chevron + system UI) — the one leftover default element; copy bug "UPDATED tonight 11:53 AM" repeated on every tab; NTPRD vs title-case rink pills. | 6.0 |
| 10 | Mobile ergonomics | Good bones: single-column stacked game cards, ~130px rows, bottom tab nav with proper safe-area, generous targets. Marred by: hero name clipped off-canvas left, "TimeHomeScoreAwayRink" concatenated header, bottom-nav overlap of last row (no bottom padding), credit text clipped "UPDATED tonig" inside nav row. | 5.5 |

## WEIGHTED TOTAL
identity 6.5x12% + type 6.0x12% + color 6.5x10% + layout 5.5x12% + density 7.0x12% + interaction 7.0x10% + motion 7.5x8% + depth 7.5x8% + consistency 6.0x8% + mobile 5.5x8% = **6.5/10**

## 3 cheapest fixes with highest score delta
1. **Fix .today-cols-head grid placement** (span both columns or duplicate header per group; hide entirely on mobile cards). One CSS rule: `grid-column: 1 / -1` + per-group header template. Delta: layout +1.5, mobile +1.0 → **+0.27 total**.
2. **Fix the hero-name absolute positioning** (remove absolute / restore to card flow, kill the "0–0 @" dangling-opponent state, hide LAST row when opponent unknown). Delta: identity +1.5, consistency +0.5 → **+0.25 total**.
3. **Kill off-system hues & unify mono labels** (replace link-blue #2A7FD4 with Union Blue #002654, amber chip with navy, flatten top gradient strip to solid Goal Red; snap all mono labels to 2 sizes at one tracking). Delta: color +1.5, type +1.0, consistency +1.0 → **+0.34 total**.

Sum of cheap fixes ≈ +0.9 → ~7.4 ceiling without rebuilding the Today header architecture and shipping a real signature viz (ppg-bar exists in CSS but renders on zero tabs).
